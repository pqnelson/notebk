%\documentclass{article}
\documentclass[oneside]{amsart}
\usepackage[12hr,us]{datetime}
\usepackage{macros}
\usepackage{notation}

\def\homeurl{\url{https://pqnelson.github.io/notebk/}}

\makeatletter
\@ifclassloaded{amsart}{
  \author{Alex Nelson}
  \email{pqnelson@gmail.com}
  \thanks{This is a page from \homeurl{}\hfil\break\indent\ignorespaces Compiled:\enspace\today\ at \currenttime\ (PST)}
  }{\author{Alex Nelson\thanks{This is a page from \homeurl{}\hfil\break\indent\;\, Compiled:\enspace\today\ at \currenttime\ (PST)}}}
\makeatother
\title{Roadtrip to Supermathematics}
\date{October 28, 2022}

\begin{document}
\maketitle


\input{tex/introduction}
\input{tex/grassmann}
\input{tex/differentiation}
\input{tex/integration}
\input{tex/bib}
\appendix
\include{tex/literature}


% Testing notation, it seems $C(\!(z)\!)$ is aesthetically most pleasing

%% Testing:

%% $\CC((x))$ plain

%% $\CC(\!(x)\!)$ with \verb#(\!(x)\!)#

%% $\CC{(}{(}x{)}{)}$ with \verb#\CC{(}{(}x{)}{)}#

%% $\CC(\mkern-2mu(z)\mkern-2mu)$ with \verb$\CC(\mkern-2mu(z)\mkern-2mu)$

%% $\CC(\mkern-2.5mu(z)\mkern-2.5mu)$ with \verb$\CC(\mkern-2.5mu(z)\mkern-2.5mu)$

%% $\CC(\mkern-3mu(z)\mkern-3mu)$ with \verb$\CC(\mkern-3mu(z)\mkern-3mu)$

\end{document}
